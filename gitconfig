[user]
	name = Diego Alberto
	email = dmalberto@outlook.com
	sslVerify = false

[core]
	editor = code

[alias]
  hist = "!f() { commits=${1:--20};  git log $commits --branches=* --decorate --oneline --graph --remotes; }; f"

[color]
	status = auto
	ui = auto
	diff = auto

[color "status"]
	added = green
	changed = yellow 
	untracked = magenta
	deleted = red